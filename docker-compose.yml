version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: pm_tool_db
    environment:
      POSTGRES_USER: ${DATABASE_USER:-pm_tool_user}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-your_secure_password}
      POSTGRES_DB: ${DATABASE_NAME:-pm_tool}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER:-pm_tool_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: pm_tool_app
    environment:
      DATABASE_USER: ${DATABASE_USER:-pm_tool_user}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:-your_secure_password}
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_NAME: ${DATABASE_NAME:-pm_tool}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      JWT_ACCESS_TOKEN_EXPIRE_DAYS: ${JWT_ACCESS_TOKEN_EXPIRE_DAYS:-7}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    command: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"

volumes:
  postgres_data:
